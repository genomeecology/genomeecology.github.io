# site properties and page defaults
title: Mobile DNA, Evolutionary Dynamics, and Genome Ecology Lab
subtitle: Naturhistoriska Riksmuseet (Swedish Museum of Natural History), Stockholm
description: We are fascinated by the mechanisms that drive genome innovation and host adaption across the tree of life. By combining comparative genomics, evolutionary theory, and computational biology, we seek to understand how mobile DNA influences genome structure, adaptation, and biodiversity across deep evolutionary time.
header: images/doubleHelix.png
footer: images/doubleHelix.png
proofer: false

# site social media and other links
links:
  email: contact@genomeecology.com
  orcid: 0000-0002-5936-7531
  google-scholar: 9CVpP6sAAAAJ
  github: genomeecology
  bluesky: tobybarilbio.bsky.social

### jekyll settings

# front matter defaults
defaults:
  # all markdown files
  - scope:
      path: ""
    values:
      layout: default
  # markdown files in /_members
  - scope:
      type: "members"
    values:
      layout: member
  # markdown files in /_posts
  - scope:
      type: "posts"
    values:
      layout: post

collections:
  # generate page for each member
  members:
    output: true
  # generate page for each post
  posts:
    output: true

# jekyll plugins
plugins:
  - jekyll-spaceship
  - jekyll-sitemap
  - jekyll-redirect-from
  - jekyll-feed
  - jekyll-last-modified-at

# code block syntax highlighting
highlighter: rouge

# jekyll theme
theme: null

# sass settings
sass:
  sass_dir: _styles

# force jekyll to include certain files/folders
include:
  - _styles
  - _scripts

# force jekyll to exclude certain files/folders
exclude:
  - README.md
  - LICENSE.md
  - CITATION.cff

# configure jekyll spaceship
jekyll-spaceship:
  mathjax-processor:
    src:
      - https://cdn.jsdelivr.net/npm/mathjax@4/tex-mml-chtml.js
